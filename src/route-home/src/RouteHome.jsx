import React from 'react';

let RouteHome = () => {
	return <React.Fragment>
		RouteHome
	</React.Fragment>;
};

RouteHome = React.memo(RouteHome);
RouteHome.defaultProps = {
};
RouteHome.propTypes = {
};

export default RouteHome;
